const num1 = 100;
const num2 = 200;

let val;

//math with numbers
val = num1 + num2;
val = num2 - num1;
val = num1 * num2;
val = num2 / num1;
val = num2 % num1;

//math object
val =Math.PI;
val = Math.pow(5,2);
val =Math.ceil(2.5);

console.log(val);
